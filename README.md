# OnchipPowerModel

TODO:
1. add conv_softmax benchmark to vpu and lsu
2. collect random testset for iu, vidu, secondary idu and ifu
3. collect random testset for 
3. retrain and store the results, this time generate a standardalone aq_core R2 VS window size graph, the aq_core's predicted power comes from the addition of all modules.

4. check the feature importance of the trained proxy to see if that makes sense, and needs to adjust the input proxies.
5. add the feature selection 
6. trian until the aq_core's test score > 0.9