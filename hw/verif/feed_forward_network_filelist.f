// HDL sources for the FeedForwardNetwork end-to-end DPI-C check (VCS -f filelist).
// The DPI glue (feed_forward_network_dpi.c) and the C model
// (src/models/feed_forward_network_cmodel.c) are passed directly on the vcs
// command line by the Makefile.
../rtl/feed_forward_network.v
tb_feed_forward_network.sv
