# Smoke config: small cases only, separate output dir.
db_root: c906_db_net_1cyc_20260729
scope: cp0
target: x_aq_core/Pc(x_aq_cp0_top)
output_dir: out/x-opm-smoke

cases: [csr, interrupt, MMU, cache]
test_benchmarks: [interrupt]

hamming_types: [C]
hamming_count_initial: true
detect_duplicates: true
dedup_identical: false
